---
# Cisco lab style test playbook.
# This confirms Ansible can run a simple command against the inventory host.
- name: Test local Ansible connection
  hosts: all
  gather_facts: false

  tasks:
    - name: Print hello world from localhost
      command: echo hello world
      changed_when: false
